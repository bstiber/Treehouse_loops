# Using the each method, iterate over every item in the numbers array and print
# the item using the puts method.

numbers = [0, 1, 2, 3, 4, 5]

numbers.each do |item|
  puts item
end

 p numbers.inspect
